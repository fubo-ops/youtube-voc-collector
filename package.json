{
  "name": "youtube-voc-collector",
  "version": "1.0.0-rc1",
  "private": true,
  "description": "Public YouTube VOC comment collector and Codex Skill",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/test_youtube_voc_core.cjs",
    "check": "node --check scripts/youtube_playwright_collector.cjs && node --check scripts/youtube_voc_core.cjs && node --check scripts/rebuild_raw_collection.cjs && node --check scripts/build_youtube_excel.mjs"
  },
  "dependencies": {
    "playwright": ">=1.55.0 <2"
  }
}
