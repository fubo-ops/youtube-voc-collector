[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "youtube-voc-collector"
version = "1.0.0rc1"
description = "Public YouTube VOC comment collector and Codex Skill for Amazon ASIN research"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = ["PyYAML>=6,<7"]

[tool.setuptools]
packages = []
